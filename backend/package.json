{
  "name": "backend",
  "version": "1.0.0",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "sanitize-html": "^2.7.0",
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "start": "npm run kill-port && node server.js",
    "kill-port": "kill-port 8000",
    "dev": "nodemon server.js"
  }
}
