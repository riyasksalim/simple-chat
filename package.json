{
  "name": "chat-app",
  "version": "1.0.0",
  "scripts": {
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "start": "npm run build --prefix frontend && npm run start --prefix backend"
  },
  "dependencies": {},
  "devDependencies": {}
}
